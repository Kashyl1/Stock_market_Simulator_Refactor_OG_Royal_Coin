spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/trading_simulator_test
    username: test
    password: test
    hikari:
      initialization-fail-timeout: -1
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        boot:
          allow_jdbc_metadata_access: false
  flyway:
    enabled: false
